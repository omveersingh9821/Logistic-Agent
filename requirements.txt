# Claims Analyzer — Dependencies
# Install: pip install -r requirements.txt

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Anthropic SDK (Claude API)
anthropic>=0.39.0

# OpenAI SDK — gpt-4o-transcribe (audio) + Responses API (gpt-4o-mini Hinglish translation)
openai>=1.66.0

# Env management
python-dotenv>=1.0.0

# Computer Vision
opencv-python-headless>=4.9.0

# OCR
pytesseract>=0.3.10

# Numerical
numpy>=1.24.0

# Image handling (used by pytesseract internally)
Pillow>=10.0.0

# ──────────────────────────────────────────────
# SYSTEM DEPENDENCIES (install separately)
# ──────────────────────────────────────────────
# sudo apt update && sudo apt install -y ffmpeg tesseract-ocr
#
# macOS:
# brew install ffmpeg tesseract
#
# Verify:
# ffmpeg -version
# tesseract --version